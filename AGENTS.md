# AGENTS.md

## Project Rules

- ALWAYS use `shadcn` for UI components and patterns.
- ALWAYS use `bun`, not `npm`.
- NEVER use `npx`; use `bunx` instead.
- Run Vercel CLI in this repo with `bunx vercel`.
- If `bun` is not on `PATH` yet, use `~/.bun/bin/bunx vercel`.
